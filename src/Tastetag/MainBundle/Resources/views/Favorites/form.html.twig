<form action="{{ path('recipe_favorite_create', { 'recipe_id' : recipe_id } ) }}" method="post" {{ form_enctype(form) }} class="recipeger">
    {{ form_rest(form) }}
        <input type="submit" value="Zapisz" >
</form>